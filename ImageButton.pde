class ImageButton{
  float x,y,w,h;
  
  ImageButton(float x,float y,PImage image, float w, float h){
    this.x = x; this.y=y; this.w = w; this.h = h;
  }
  // maybe not needed for first page since all the button is doing is clearing the page
  
}
